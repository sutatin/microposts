<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%# 本来ならselect要素とかにしたい。時間ないので省略 %>
      <%= f.label :region %>
      <%= f.text_field :region %>

      <%# 本来ならテキストエリア要素とかにしたい。時間ないので省略 %>
      <%= f.label :profile %>
      <%= f.text_field :profile %>

      <%# 本来ならパスワードは動きが別なので別画面にしたい。でも時間ないので省略… %>
      <%# そもそも何でpasswordは空でもpostした時に受け付けられているかは謎 %>
      <%= f.label :password %>
      <%= f.password_field :password %>

      <%= f.label :password_confirmation, "Confirmation" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit @submitMessage , class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>